<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ELeaner Welocome</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            -webkit-text-size-adjust: none;
            /* Prevent iOS font size adjustment */
            width: 100% !important;
            /* Ensure full width on mobile */
        }

        .container {
            max-width: 600px;
            margin: 20px auto;
            background-color: #ffffff;
            border: 1px solid #e0e0e0;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.05);
        }

        .header {
            background-color: #007bff;
            padding: 25px;
            text-align: center;
            color: #ffffff;
        }

        .header h1 {
            margin: 0;
            font-size: 28px;
        }

        .content {
            padding: 30px;
            color: #555555;
            line-height: 1.6;
        }

        .content p {
            font-size: 16px;
            margin-bottom: 15px;
        }

        .otp-code {
            font-size: 22px;
            font-weight: bold;
            color: #010b03;
            /* Green color for OTP */
            background-color: #dce3de;
            padding: 10px 15px;
            border-radius: 6px;
            margin: 25px 0;
            letter-spacing: 5px;
            border: 1px dashed #cccccc;
        }

        .footer {
            background-color: #f8f8f8;
            padding: 20px;
            text-align: center;
            font-size: 12px;
            color: #999999;
            border-top: 1px solid #e0e0e0;
        }

        .footer p {
            margin: 0;
        }

        /* Responsive adjustments */
        @media screen and (max-width: 600px) {
            .container {
                margin: 10px;
                border-radius: 0;
            }

            .header,
            .content,
            .footer {
                padding: 20px;
            }

            .otp-code {
                font-size: 38px;
                padding: 10px 20px;
            }

            .header h1 {
                font-size: 24px;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="header">
            <h1>Welcome to ELearn</h1>
        </div>
        <div class="content">
            <p>Hello <strong style="color: #007bff;">
                    <%= user.name %>
                </strong>,</p>
            <p>Thank you for using our service. Your One-Time Password (OTP) for verification is:</p>
            <div class="otp-code">
                <%= activationToken %>
            </div>
            <p>This OTP is valid for 5 minutes.</p>
            <p>If you did not request this, please ignore this email.</p>
        </div>
        <div class="footer">
            <p>&copy; <%= new Date().getFullYear() %> ELearner. All rights reserved.</p>
            <p><a href="#" style="color: #007bff; text-decoration: none;">Privacy Policy</a> | <a href="#"
                    style="color: #007bff; text-decoration: none;">Terms of Service</a></p>
        </div>
    </div>
</body>

</html>